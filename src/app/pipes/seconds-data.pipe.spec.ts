import { SecondsDataPipe } from './seconds-data.pipe';

describe('SecondsDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsDataPipe();
    expect(pipe).toBeTruthy();
  });
});
